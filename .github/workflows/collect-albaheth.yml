name: Disabled — Albaheth bulk collection not permitted

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prohibited_bulk_extraction:
    runs-on: ubuntu-latest
    steps:
      - name: Record prohibition
        run: |
          echo 'Albaheth bulk extraction is disabled.'
          echo 'The platform terms prohibit automated bulk data extraction.'
          echo 'Use only authorized original URLs from the Ministry of Justice.'
